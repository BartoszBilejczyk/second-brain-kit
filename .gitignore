# Generated embeddings — rebuild locally with: python tools/embed-wiki.py
meta/embeddings.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# JetBrains IDEs (PyCharm, WebStorm, IntelliJ, etc.)
.idea/
*.iml
*.iws
*.ipr

# VS Code
.vscode/

# Python cache
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Node / npm (if any tooling is added)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Sentence-transformers model cache (downloaded on first embed run)
.cache/

# Whisper model cache (stored in ~/.cache, not the repo)
# — no action needed, listed here for clarity

# Audio recordings — kept local only, not pushed to GitHub
interview/recordings/

# Raw personal content — not pushed to public template forks
raw/transcripts/
raw/sources/
