# Generated embeddings — rebuild locally with: python tools/embed-wiki.py
meta/embeddings.json

# macOS
.DS_Store

# Python cache
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# Whisper model cache (stored in ~/.cache, not the repo)
# — no action needed, listed here for clarity

# Audio recordings — kept local only, not pushed to GitHub
interview/recordings/
